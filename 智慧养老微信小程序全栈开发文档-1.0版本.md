# 智慧养老微信小程序 — 1.0 版本全量开发文档

> 项目代号：smart_elderly_care  
> 版本：v1.0  
> 定位：面向老年人、子女、社区服务人员三方联动的智慧养老微信小程序  
> 最后更新：2026-09-16

---

## 一、项目概述与核心特点

### 1.1 项目定位

本项目是一个**原生微信小程序 + Python 后端**的智慧养老系统，通过角色切换呈现**老人端、子女端、社区端**三套视图，三端数据实时联动。核心目标是让老人获得便捷的健康管理与情感陪伴，让子女远程掌握老人状况，让社区高效处理服务工单。

### 1.2 核心特点

| 特点 | 说明 |
|---|---|
| **三端一体** | 一个小程序，启动页选择身份（老人/子女/社区），底部 tabBar 统一，首页按角色渲染不同内容 |
| **双 AI 助手** | 健康助手（代码层智能体，RAG+Function Calling+术语树）+ 陪伴助手（轻量 API 调用，情感陪伴） |
| **真实 AI 回复** | 所有 AI 回复均来自后端调用大模型，前端不做规则匹配或模拟回复 |
| **语音交互** | 支持语音输入（ASR 语音转文字）和语音播报（TTS 文字转语音），方言适配（四川话/东北话） |
| **视频监控+跌倒检测** | 后端跑 YOLOv8 跌倒检测模型，前端仅展示画面，检测到跌倒自动告警并截图 |
| **适老化设计** | 大按钮（≥88rpx）、大字（正文≥32rpx）、大间距、操作步骤≤3步、重要操作二次确认 |
| **术语树形解释** | 子女端健康助手回答中标注专业术语，点击展开独立分支解释，支持两层嵌套，不污染主线上下文 |
| **本地持久化** | 对话历史、用药记录、告警、日记等均支持本地 storage + 数据库双重持久化 |
| **多环境隔离** | 业务后端用 base 环境，视频/跌倒检测用 deeplearning 环境，互不污染 |

---

## 二、技术栈

### 2.1 前端

| 类别 | 技术 |
|---|---|
| 框架 | 原生微信小程序（WXML + WXSS + JS） |
| 自定义组件 | custom-tab-bar（底部导航按角色切换） |
| 网络请求 | 封装 utils/request.js |
| 音频播放 | wx.createInnerAudioContext |
| 语音录制 | wx.getRecorderManager |
| 本地存储 | wx.setStorageSync / wx.getStorageSync |
| 图片资源 | 57 张真实图片（assets/images/） |

### 2.2 后端

| 类别 | 技术 |
|---|---|
| 业务后端框架 | FastAPI + Uvicorn |
| 视频流后端框架 | Flask |
| 跌倒检测服务 | FastAPI + Ultralytics YOLOv8 + OpenCV |
| 数据库 | SQLite（smart_elderly_care.db） |
| ORM | SQLAlchemy |
| 大模型 API | 阿里云 DashScope（Qwen-Flash，支持 thinking 模式） |
| ASR/TTS | 阿里云语音交互（Paraformer 语音识别 + CosyVoice 语音合成） |
| 向量检索 | 本地轻量 RAG（TF-IDF + 关键词匹配，无需向量数据库） |
| 定时任务 | APScheduler |
| 视频推流 | WebSocket（/ws/stream）+ JPEG 帧推送 |

---

## 三、前端架构

### 3.1 目录结构

```
smart_elderly_care_miniprogram/
├── app.js                  # 全局入口，角色状态管理
├── app.json                # 页面注册、tabBar 配置、窗口样式
├── app.wxss                # 全局样式（CSS 变量、适老化基础样式）
├── project.config.json     # 项目配置
├── sitemap.json            # 站点地图
├── custom-tab-bar/         # 自定义底部导航（按角色显示不同 tab）
├── assets/
│   ├── images/             # 57 张真实图片（Banner、医生、家人、机构等）
│   ├── music/              # 6 首舒缓音乐（mp3）
│   └── audiobook/          # 听书戏曲音频（mp3，原 ogg 已转码）
├── pages/                  # 所有页面（详见 3.2）
├── services/               # API 服务层（详见 3.3）
├── utils/                  # 工具函数（详见 3.4）
└── images/                 # 额外图片资源
```

### 3.2 页面详解（共 15 个页面模块，50+ 子页面）

#### 3.2.1 启动与身份选择

| 页面 | 路径 | 功能 |
|---|---|---|
| 启动页 | `pages/splash/splash` | 选择身份（我是老人/我是家属/我是社区服务人员），写入全局状态和本地 storage |

#### 3.2.2 老人端

| 页面 | 路径 | 功能 |
|---|---|---|
| **老人首页** | `pages/index/index` | 智慧养老品牌标题 + 搜索栏（服务搜索/问医生）+ Banner 轮播（6张）+ 公告滚动条（小喇叭）+ 天气农历节气卡片（jieqi.png 背景）+ 常用服务（一键呼救/智能用药/健康问答/健康记录）+ 今日吃药进度条 + 常联系人横滑（儿子/女儿/社区王师傅，可拨号+留言）+ 在线问诊医生卡片（5位医生）+ 社区活动（义诊/书法）+ 附近便民（便利店/药店/菜市场/社区服务）+ 健康知识（高血压/失眠/饮食）+ 养老政策资讯 + 未读消息提醒 + AI 悬浮球（可拖动，吸附左右边缘，切换健康/陪伴助手） |
| **健康主页** | `pages/health/main/main` | 健康管理总入口，顶部"健康管理"大标题 + 在线问诊 + 医院就医攻略 + 健康记录 + 用药提醒 + 健康知识 |
| **健康助手** | `pages/health/assistant/assistant` | 健康 AI 对话页，老人端口语化回复，子女端专业汇报+术语标注，支持语音输入/语音播报，预设问题快捷入口（上下两行流动滚动），流式输出，清空对话 |
| **术语解释** | `pages/health/explain/explain` | 专业术语独立分支解释页，专业性高，支持再嵌套一层术语解释（最多两层），robot-2 头像，不调用数据工具 |
| **在线问诊** | `pages/health/consult/consult` | 医生列表（5位医生，真实头像），点击进入医生对话页 |
| **医生对话** | `pages/health/consult/doctor/doctor` | 与指定医生的模拟对话，预设问题快捷发送，医生固定回复，老人可发信息（不接后端真实医疗服务） |
| **医院攻略** | `pages/health/hospital/hospital` | 医院列表（4家医院真实图片），挂号攻略、就医流程、科室介绍，搜索栏（平面放大镜图标，蓝色配色） |
| **用药提醒** | `pages/health/medicine/medicine` | 今日用药计划（早/中/晚分组），每条显示药名/剂量/时间/状态（待提醒/已吃药/未确认/漏服），点击已吃药状态变更+语音播报，手动添加药品，语音播报今日用药，顶部今日完成进度 |
| **健康记录** | `pages/health/record/record` | 血压/血糖/心率/体重数据录入，趋势图+大数字展示，异常值红色标记，最近7次记录 |
| **联系家人** | `pages/contact/contact` | 绑定的子女头像（儿子/女儿），点击模拟拨号/视频通话/语音留言，亲情绑定码 |
| **家人对话** | `pages/contact/chat/chat` | 与子女的文字+语音对话，消息持久化，未读计数 |
| **社区工人对话** | `pages/contact/worker/worker` | 与社区王师傅的对话，独立于家人对话，消息不混淆 |
| **附近便民** | `pages/nearby/nearby` | 4分类tab（便利店/药店/菜市场/社区服务），每类3个模拟店铺（真实图片+地址+距离+营业时间），一键拨号 |
| **社区活动** | `pages/activity/activity` | 活动列表（义诊/书法/健走/讲座，真实图片），活动详情页 |
| **健康知识详情** | `pages/knowledge/detail/detail` | 高血压/失眠/糖尿病饮食三篇详细知识，大图半隐背景+滑动效果，点击查看完整图片 |
| **新闻资讯** | `pages/news/index/index` | 养老政策/资讯列表，点击查看详情 |
| **新闻详情** | `pages/news/detail/detail` | 资讯详情页 |
| **视频监控** | `pages/monitor/monitor` | 老人端监控画面展示（后端 WebSocket 推流），跌倒检测状态 |
| **个人中心** | `pages/profile/profile` | 老人资料卡（头像/姓名/绑定关系），切换身份，重置模拟数据，消息设置，关于小程序，系统设置（夜间模式/字体大小），编辑资料入口 |
| **个人资料详情** | `pages/profile/detail/detail` | 二级页面，可编辑个人信息（姓名/性别/年龄/血型/慢病/过敏史/紧急联系人/家庭地址等） |
| **提醒设置** | `pages/profile/reminder/reminder` | 用药提醒设置，新建/编辑提醒 |
| **语音设置** | `pages/profile/voice/voice` | 语音方言选择（普通话/四川话/东北话），TTS 音色设置 |

#### 3.2.3 子女端

| 页面 | 路径 | 功能 |
|---|---|---|
| **子女首页** | `pages/daughter/home/home` | 老人状态卡片（今日吃药完成情况/最近血压/最近血糖/有无告警/最后活动时间）+ 今日告警列表（未确认吃药/血压偏高/SOS求助/久未活动）+ 快捷操作（帮老人设提醒/代下单/视频通话/查看老人详情）+ 健康助手入口 + 养老机构服务入口 + AI 悬浮球 |
| **告警列表** | `pages/daughter/alerts/alerts` | 按时间倒序所有告警，状态（未处理/已处理/已忽略），点击查看详情/联系老人/通知社区/标记处理，处理后保留已处理记录 |
| **老人详情** | `pages/daughter/detail/detail` | 老人档案/健康记录/用药计划/位置信息/紧急联系人/绑定关系，可编辑部分信息，历史告警和处理记录 |
| **服务代付** | `pages/daughter/services/services` | 养老机构/保洁/陪诊等服务选择，远程为老人下单，模拟支付 |
| **养老机构** | `pages/daughter/order/order` | 养老机构列表（4家机构真实图片），机构详情，预约参观 |
| **子女对话** | `pages/daughter/chat/chat` | 与老人的对话，接收老人消息和语音留言，回复老人 |
| **子女个人中心** | `pages/daughter/mine/mine` | 子女资料卡（李女士，真实头像），切换身份，编辑资料，系统设置 |
| **子女资料编辑** | `pages/daughter/profile/profile` | 二级页面，可编辑个人信息 |
| **视频监控** | `pages/monitor/monitor` | 子女端监控画面（功能最全），跌倒记录列表（截图+时间+日期），点击跌倒记录查看详情，弹窗警示 |

#### 3.2.4 社区端

| 页面 | 路径 | 功能 |
|---|---|---|
| **社区首页** | `pages/community/home/home` | 工单列表（待接单/服务中/已完成分组），紧急 SOS 工单置顶标红，工单卡片（老人姓名/服务类型/预约时间/地址/状态） |
| **工单详情** | `pages/community/order-detail/order-detail` | 工单详情，接单/出发/完成服务/上传模拟照片/填写服务记录，完成后老人端收到确认提醒，子女端看到服务完成 |
| **老人档案** | `pages/community/elders/elders` | 社区老人档案列表，点击查看老人基本信息/健康摘要/用药情况/家属联系方式/历史工单 |
| **告警处理** | `pages/community/alert/alert` | SOS 和异常告警列表，标记已联系/已上门/已处理，处理结果同步子女端 |
| **社区消息** | `pages/community/messages/messages` | 新工单/告警通知，老人留言入口（复用子女端对话逻辑），未读红点 |
| **老人留言** | `pages/community/visit/visit` | 接收老人发送的消息，回复老人，独立于其他对话通道 |
| **社区条例** | `pages/community/rules/rules` | 社区服务规则/条例展示，模拟规则列表 |
| **条例详情** | `pages/community/rules/detail/detail` | 条例详情页 |
| **服务评价** | `pages/community/evaluation/evaluation` | 服务评价查看 |
| **社区个人中心** | `pages/community/mine/mine` | 社区工作人员资料卡（真实头像），切换身份，编辑资料 |
| **社区资料编辑** | `pages/community/profile/profile` | 二级页面，可编辑个人信息 |

#### 3.2.5 关爱陪伴模块（老人端入口）

| 页面 | 路径 | 功能 |
|---|---|---|
| **陪伴主页** | `pages/chat/chat` | 关爱陪伴品牌头 + 动态统计（照片数/日记数/音乐数）+ 今日推荐音乐卡片 + 每日暖心语录（每天一条，14条轮换）+ 精彩内容入口（家庭相册/健脑游戏/心情日记/舒缓音乐/听书戏曲）+ AI 陪聊助手入口 + AI 悬浮球 |
| **陪伴助手对话** | `pages/chat/talk/talk` | 陪伴 AI 对话页，孙子角色，情感陪伴/聊天解闷/故事戏曲/天气新闻，支持语音输入/语音播报（按需生成，不预合成），本地 storage 持久化（最多50条），清空对话，联网搜索权威信息（新闻/生活常识），不涉及医学健康知识（引导至健康助手） |
| **家庭相册** | `pages/companion/album/album` | 2列网格，4张家人照片（儿子/女儿/奶奶/家人），自定义全屏预览（swiper+左右滑动+页码+关闭） |
| **健脑游戏选择** | `pages/companion/game/index/index` | 5个游戏入口卡片 |
| **翻牌记忆** | `pages/companion/game/memory/memory` | 三关难度（2x2→3x4→4x4），配对成功淡出消除 |
| **趣味数独** | `pages/companion/game/sudoku/sudoku` | 4x4简化版，三关难度（空格递增），提示功能 |
| **成语接龙** | `pages/companion/game/idiom/idiom` | 选择题形式，15题库每轮随机10题 |
| **对对消** | `pages/companion/game/match/match` | 点两个相同图案消除，三关（2x3/4x3/4x4），提示功能 |
| **算术小能手** | `pages/companion/game/math/math` | 100以内加减法，三关（20/50/100以内），选择题 |
| **心情日记** | `pages/companion/diary/diary` | 5种心情（开心/平静/难过/生气/疲惫），底部弹出面板写日记，历史列表按时间倒序，筛选栏，删除（二次确认），本地 storage 持久化 |
| **舒缓音乐** | `pages/companion/music/music` | 6首音乐（海底/夜萤火虫和你/PDD洪荒之力/樱花泪/画心/青衣），完整播放器（播放/暂停/上一首/下一首/进度条拖拽/收藏/顺序单曲循环），每次播放新建音频实例杜绝曲目混淆 |
| **听书戏曲** | `pages/companion/audiobook/audiobook` | 3分类（经典名著/评书相声/传统戏曲），每类4个曲目，完整播放器，阿东读历史+帝女花已适配真实音频（ogg转mp3），每次播放新建音频实例 |

### 3.3 services 层（API 服务）

| 文件 | 功能 |
|---|---|
| `services/agent-api.js` | AI 助手接口封装（健康助手/陪伴助手对话、ASR 语音识别、TTS 语音合成、术语解释） |
| `services/alert-api.js` | 告警接口（创建告警、获取告警列表、标记处理） |
| `services/community.js` | 社区端接口（工单、老人档案、服务记录） |
| `services/news.js` | 新闻资讯数据（本地 mock，养老政策/资讯列表） |
| `services/orders.js` | 订单接口（服务预约、代付、订单状态） |

### 3.4 utils 工具

| 文件 | 功能 |
|---|---|
| `utils/request.js` | 封装 wx.request，统一 baseURL、错误处理、超时 |
| `utils/config.js` | 全局配置（API 地址、角色常量等） |
| `utils/music-config.js` | 音乐列表公共配置（6首音乐，陪伴主页和音乐播放器共用） |
| `utils/album-config.js` | 家庭相册照片公共配置（4张家人照片） |

---

## 四、后端架构

后端由**三个独立服务**组成，分别运行在不同环境：

### 4.1 目录结构

```
houduan/
├── smart_elderly_care/          # FastAPI 业务后端（端口 8000）
│   ├── main.py                   # 入口，注册路由，启动 APScheduler
│   ├── requirements.txt          # Python 依赖
│   ├── smart_elderly_care.db    # SQLite 数据库
│   ├── static/                   # 静态资源（跌倒截图等）
│   └── app/
│       ├── __init__.py
│       ├── config/config.py      # 配置（API Key、模型、数据库路径）
│       ├── models/               # SQLAlchemy 数据模型（详见 4.3）
│       ├── schemas/              # Pydantic 请求/响应模型
│       ├── routers/              # API 路由（详见 4.2）
│       ├── services/             # 业务逻辑层（详见 4.2）
│       └── utils/                # 工具（LLM、ASR、TTS、语音）
├── smart_elderly_care_backend/   # Flask 视频流后端（端口 5001）
│   ├── app.py                    # 视频监控页面 + API 转发
│   ├── requirements.txt
│   └── elderly_care.db
├── Fall-Down-Det/                # 跌倒检测服务（独立进程，端口由 detector_server 决定）
│   ├── detector_server.py        # YOLOv26 跌倒检测 + WebSocket 推流 + 告警上报
│   ├── yolo26n.pt / yolov8n.pt  # 模型权重
│   ├── runs/train/yolo26n_fall/weights/best.pt  # 训练好的跌倒检测模型
│   ├── alert_shots/              # 跌倒截图保存目录
│   ├── FallDataset-yolo/         # 训练数据集
│   └── scripts/                   # 辅助脚本
├── start_all.ps1                 # 一键启动脚本（启动业务后端+视频后端）
└── stop_all.ps1                  # 一键停止脚本
```

### 4.2 FastAPI 业务后端（端口 8000）

#### 4.2.1 路由层（routers/）

| 文件 | 功能 |
|---|---|
| `users.py` | 用户注册/登录/角色切换/资料编辑/绑定关系 |
| `health.py` | 健康记录 CRUD（血压/血糖/心率/体重） |
| `health_agent.py` | 健康助手对话 API（核心智能体入口） |
| `companion.py` | 陪伴助手对话 API（轻量 AI） |
| `medicine.py` | 用药计划 CRUD |
| `medicine_log.py` | 用药确认记录（已吃/漏服） |
| `alerts.py` | 告警管理（创建/列表/标记处理/SOS/跌倒/漏服） |
| `messages.py` | 消息中心（三端消息通道：family/notice/worker） |
| `orders.py` | 服务订单（预约/代付/状态流转） |
| `asr.py` | 语音识别（ASR，支持方言） |
| `tts.py` | 语音合成（TTS，CosyVoice，支持方言音色） |
| `test.py` | 测试接口 |

#### 4.2.2 服务层（services/）

| 文件 | 功能 |
|---|---|
| `agent/agent.py` | **健康智能体核心编排器**：安全层 + RAG + Function-Calling 工具循环 + 多轮记忆 + 术语标注（详见第五章） |
| `agent/companion.py` | 陪伴助手服务：轻量 LLM 调用，孙子角色，情感陪伴，联网搜索 |
| `agent/health_tools.py` | 健康助手工具实现（8个工具：查血压/查血糖/查心率/查体重/查今日用药/查告警/查健康档案/搜索知识库） |
| `agent/tool_specs.py` | 工具 Function-Calling 规格定义（DashScope 格式） |
| `agent/knowledge_service.py` | RAG 知识库服务（21篇txt→41片段，TF-IDF检索） |
| `agent/knowledge_seed.py` | 知识库种子数据初始化 |
| `agent/vector_store.py` | 轻量向量存储（本地实现，无需外部向量数据库） |
| `agent/embedding.py` | 文本嵌入（关键词+TF-IDF，轻量实现） |
| `agent/knowledge/` | 知识库 txt 文件目录（21篇健康科普文档） |
| `user_service.py` | 用户业务逻辑 |
| `health_service.py` | 健康记录业务逻辑 |
| `medicine_service.py` | 用药计划业务逻辑 |
| `medicine_log_service.py` | 用药确认业务逻辑 |
| `alert_service.py` | 告警业务逻辑 |
| `message_service.py` | 消息业务逻辑 |
| `order_service.py` | 订单业务逻辑 |
| `reminder_service.py` | 定时提醒服务（APScheduler） |
| `reminder_setting_service.py` | 提醒设置业务逻辑 |
| `seed_data.py` | 初始模拟数据（老人/子女/社区/用药/健康记录等） |
| `migrate.py` | 数据库迁移 |

#### 4.2.3 工具层（utils/）

| 文件 | 功能 |
|---|---|
| `llm.py` | 大模型调用封装（DashScope Qwen-Flash，支持 thinking 模式，流式输出 SSE，紧急关键词拦截） |
| `asr.py` | 语音识别（阿里云 Paraformer，支持四川话/东北话方言） |
| `tts.py` | 语音合成（阿里云 CosyVoice v3-flash，支持方言音色，强制模型版本） |
| `speech.py` | 语音处理工具（音频格式转换、音量处理） |

### 4.3 数据库模型（models/）

| 模型 | 表名 | 功能 |
|---|---|---|
| `User` | users | 用户表（老人/子女/社区，角色、姓名、头像、绑定关系、资料） |
| `HealthRecord` | health_records | 健康记录表（血压/血糖/心率/体重，时间戳，异常标记） |
| `Medicine` | medicines | 用药计划表（药名、剂量、服用时间、重复周期） |
| `MedicineLog` | medicine_logs | 用药确认记录（已吃/未确认/漏服，时间） |
| `Alert` | alerts | 告警表（SOS/跌倒/漏服/血压异常，状态，处理记录，截图） |
| `Message` | messages | 消息表（三端通道 family/notice/worker，发送者，内容，已读状态） |
| `Order` | orders | 订单表（服务类型，预约时间，地址，状态流转） |
| `Reminder` | reminders | 提醒设置表 |
| `Conversation` | conversations | 对话会话表（健康助手/陪伴助手，角色，老人ID） |
| `ChatMessage` | chat_messages | 对话消息表（角色，内容，时间，术语标注） |
| `Knowledge` | knowledge | 知识库表（标题，内容，分类，片段） |
| `database.py` | - | 数据库连接与初始化 |

---

## 五、智能体双助手部署（重点）

### 5.1 架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    微信小程序前端                          │
│  ┌──────────────┐          ┌──────────────┐            │
│  │  健康助手入口  │          │  陪伴助手入口  │            │
│  │ (悬浮球/健康页)│          │ (悬浮球/陪伴页)│            │
│  └──────┬───────┘          └──────┬───────┘            │
└─────────┼───────────────────────────┼────────────────────┘
          │ POST /api/health-agent     │ POST /api/companion
          │ POST /api/explain          │
┌─────────▼───────────────────────────▼────────────────────┐
│                  FastAPI 业务后端                           │
│  ┌─────────────────────────┐  ┌─────────────────────────┐│
│  │    健康助手（代码层智能体）│  │    陪伴助手（轻量 API）   ││
│  │  agent/agent.py          │  │  agent/companion.py      ││
│  │  ┌─────────────────────┐ │  │  ┌─────────────────────┐ ││
│  │  │ 安全层（急症拦截）    │ │  │  │ 孙子角色 System Prompt│ ││
│  │  ├─────────────────────┤ │  │  ├─────────────────────┤ ││
│  │  │ RAG 知识库检索       │ │  │  │ 轻量 LLM 调用        │ ││
│  │  ├─────────────────────┤ │  │  ├─────────────────────┤ ││
│  │  │ Function Calling     │ │  │  │ 联网搜索（新闻/常识） │ ││
│  │  │ (8个工具循环)        │ │  │  ├─────────────────────┤ ││
│  │  ├─────────────────────┤ │  │  │ 医学健康引导至健康助手 │ ││
│  │  │ 多轮记忆（数据库）    │ │  │  └─────────────────────┘ ││
│  │  ├─────────────────────┤ │  │  语音：按需生成（不预合成）││
│  │  │ 术语标注+树形解释    │ │  └─────────────────────────┘│
│  │  ├─────────────────────┤ │                               │
│  │  │ 思考模式（仅子女端）  │ │                               │
│  │  └─────────────────────┘ │                               │
│  └─────────────────────────┘  └─────────────────────────┘│
│                          │                                   │
│              ┌───────────▼───────────┐                     │
│              │   阿里云 DashScope API  │                     │
│              │   Qwen-Flash (thinking) │                     │
│              │   + ASR + TTS          │                     │
│              └─────────────────────────┘                     │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 健康助手（代码层智能体）

#### 5.2.1 核心定位

健康助手是**代码层完整智能体**，不依赖任何 Agent 框架，用 DashScope 原生 function calling 手写推理-工具循环。这是整个项目的技术亮点，适合简历展示。

#### 5.2.2 工作流程

```
用户提问
   │
   ▼
┌─────────────────┐
│  1. 安全层检查   │  检测急症关键词（胸痛/晕倒/呼吸困难等）
│  （EMERGENCY_   │  命中 → 直接返回紧急建议，不调用 LLM
│   KEYWORDS）     │
└────────┬────────┘
         │ 未命中
         ▼
┌─────────────────┐
│  2. 构建 System  │  根据角色（老人/子女/术语解释）选择
│     Prompt       │  不同的人设和表达规则
└────────┬────────┘
         ▼
┌─────────────────┐
│  3. RAG 检索     │  从知识库（21篇txt→41片段）检索
│  （按需触发）     │  相关健康科普资料，作为参考上下文
└────────┬────────┘
         ▼
┌─────────────────┐
│  4. LLM 调用     │  DashScope Qwen-Flash
│  （带工具定义）   │  老人端：普通模式
│                  │  子女端：thinking 模式（深度思考）
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
  工具调用    直接回答
    │         │
    ▼         │
┌─────────┐  │
│ 5.执行工具│  │
│ (8选1)   │  │
└────┬────┘  │
     │       │
     ▼       │
  工具结果    │
     │       │
     └───┬───┘
         ▼
┌─────────────────┐
│  6. 术语标注     │  子女端：回答中标注 {{术语}}（最多2个）
│  （仅子女端）    │  老人端：不标注，口语化
└────────┬────────┘
         ▼
┌─────────────────┐
│  7. 流式输出     │  SSE 流式返回前端
│  （SSE）        │
└────────┬────────┘
         ▼
┌─────────────────┐
│  8. 持久化       │  对话消息写入数据库
│  （数据库）      │  支持多轮记忆（最近 AGENT_HISTORY_TURNS 轮）
└─────────────────┘
```

#### 5.2.3 三套动态人设

| 人设 | 适用场景 | 特点 |
|---|---|---|
| **老人端人设** | 老人与健康助手对话 | 口语化、短句、像家人聊天，避免专业术语，非用不可的术语打比方，回答≤150字，结尾告诉老人下一步怎么做 |
| **子女端人设** | 子女与健康助手对话 | 称呼"您"，所有健康状况主体是老人本人（"张奶奶的血压是…"），先一句总体判断，再列关键数值与变化趋势，然后给风险提示和照护建议，200~300字，可使用专业术语，标注{{术语}} |
| **术语解释人设** | 点击术语展开的分支对话 | 专业、严谨，开头一句医学定义，说明正常值范围、为什么重要、家属怎么观察，不点名具体老人，不限字数，不调用数据工具，必须再标一个{{嵌套术语}}（最多两层） |

#### 5.2.4 Function Calling 工具（8个）

| 工具名 | 功能 | 调用场景 |
|---|---|---|
| `get_blood_pressure` | 查询老人最近血压记录 | 用户问血压相关问题 |
| `get_blood_sugar` | 查询老人最近血糖记录 | 用户问血糖相关问题 |
| `get_heart_rate` | 查询老人最近心率记录 | 用户问心率相关问题 |
| `get_weight` | 查询老人最近体重记录 | 用户问体重相关问题 |
| `get_today_medicine` | 查询今日用药计划和完成情况 | 用户问吃药/用药相关问题 |
| `get_alerts` | 查询未处理告警列表 | 用户问告警/异常相关问题 |
| `get_health_profile` | 查询老人健康档案（血型/慢病/过敏史/紧急联系人） | 需要老人基础信息时 |
| `search_knowledge` | 搜索健康知识库 | 需要健康科普资料时（RAG 入口） |

**工具循环**：LLM 返回工具调用 → 后端执行工具 → 工具结果回传 LLM → LLM 基于工具结果生成最终回答。最多循环 `AGENT_MAX_TOOL_ROUNDS` 轮，防止无限调用。

#### 5.2.5 RAG 知识库

- **知识库来源**：21 篇健康科普 txt 文档（高血压、糖尿病、失眠、饮食、用药安全等）
- **切片策略**：按段落切片，共 41 个片段
- **检索方式**：轻量 TF-IDF + 关键词匹配（无需外部向量数据库，本地实现）
- **触发方式**：通过 `search_knowledge` 工具按需触发，不每次都检索
- **嵌入实现**：`agent/embedding.py` 本地轻量实现，不调用外部 embedding API
- **向量存储**：`agent/vector_store.py` 本地内存存储，启动时加载

#### 5.2.6 术语树形解释（创新功能）

这是本项目的**对话创新点**，解决普通 Chat 解释术语后上下文被稀释的问题。

```
主线对话 A（子女端）
  │
  ├─ AI 回答："张奶奶的{{收缩压}}偏高，建议关注{{心率}}"
  │                         │
  │                    点击 {{收缩压}}
  │                         ▼
  │              分支对话 B（独立，不污染主线）
  │              "收缩压是心脏收缩时血液对血管壁的压力...
  │               正常值范围 90-140mmHg...
  │               长期偏高会造成{{靶器官}}损害"
  │                                    │
  │                              点击 {{靶器官}}
  │                                    ▼
  │                          分支对话 C（第二层，最多两层）
  │                          "靶器官是指长期高血压会损害的器官..."
  │
  └─ 主线 A 记忆完整保留，随时切回
```

**关键设计**：
- 分支对话完全独立，不继承主线上下文，主线也不读取分支内容
- 最多两层嵌套（防止无限展开和 token 爆炸）
- 术语解释页不调用数据工具，只做纯科普
- 正在解释的术语本身不再标注，必须标注一个新的嵌套术语
- 前端用独立页面 `pages/health/explain/explain` 展示，robot-2 头像

#### 5.2.7 思考模式

- **仅子女端主线开启**：Qwen-Flash thinking 模式，深度思考后给出专业回答
- **老人端不开启**：老人端需要快速、口语化回复，不需要深度思考
- **术语解释页不开启**：术语解释是固定模式，不需要思考
- **思考过程自动展开**：前端流式展示思考过程，对话框跟随思考滚动

#### 5.2.8 多轮记忆

- 对话消息持久化到 SQLite 数据库（`Conversation` + `ChatMessage` 表）
- 每次调用 LLM 时带上最近 `AGENT_HISTORY_TURNS` 轮对话作为上下文
- 前端也有本地缓存（刷新不丢）
- 支持清空对话（重新开始）

### 5.3 陪伴助手（轻量 API）

#### 5.3.1 核心定位

陪伴助手是**轻量 LLM API 调用**，不做复杂的 Function Calling 和 RAG，专注于情感陪伴。定位是"孙子角色"，像家人一样陪老人聊天。

#### 5.3.2 与健康助手的区别

| 维度 | 健康助手 | 陪伴助手 |
|---|---|---|
| 架构 | 代码层完整智能体（RAG+FC+工具循环+术语树） | 轻量 LLM API 直接调用 |
| 角色 | 健康顾问（专业） | 孙子（情感陪伴） |
| Function Calling | 8个工具，按需调用 | 无 |
| RAG | 有（健康知识库） | 无（联网搜索替代） |
| 术语标注 | 子女端有，树形解释 | 无 |
| 思考模式 | 子女端开启 | 不开启 |
| 语音播报 | 预合成（回复生成时同时生成语音，秒播放） | 按需生成（用户点播放才生成，不预合成） |
| 内容边界 | 只做健康咨询，不做情感聊天 | 情感陪伴/故事/新闻/生活常识，**不涉及医学健康知识**（引导至健康助手） |
| 联网搜索 | 不联网（用知识库） | 联网搜索（新闻/生活常识/权威信息，找不到不建议回复） |
| 持久化 | 数据库 + 前端缓存 | 前端本地 storage（最多50条） |

#### 5.3.3 陪伴助手能力

- **情感陪伴**：聊天解闷、关怀问候、识别老人情绪并及时安慰
- **故事戏曲推荐**：讲故事、推荐戏曲/评书
- **天气新闻播报**：联网搜索最新天气和新闻
- **生活常识**：联网搜索生活技巧、日常饮食（煮菜/食谱），涉及功效引导至健康助手
- **逗老人开心**：笑话、脑筋急转弯（可编造，不涉及严谨信息）
- **语音交互**：支持语音输入和语音播报（按需生成，不预合成，节省 API 成本）

#### 5.3.4 内容安全边界

- **不涉及医学健康知识**：老人问健康问题时，引导"这个问题健康助手更专业哦，我帮您转过去"
- **严谨信息联网搜索**：新闻、生活常识、天气等必须联网搜索权威信息，找不到不建议回复，防止误导老人
- **娱乐内容可编造**：故事、笑话等娱乐内容可以编造
- **日常饮食白名单**：日常煮菜/食谱可以聊，涉及健康功效引导至健康助手

### 5.4 语音交互（ASR + TTS）

#### 5.4.1 ASR 语音识别

- **服务商**：阿里云 Paraformer
- **支持方言**：普通话、四川话、东北话（粤语因混合普粤效果不佳已放弃）
- **输入格式**：mp3（微信小程序录音格式）
- **应用场景**：老人端语音输入（健康助手/陪伴助手）

#### 5.4.2 TTS 语音合成

- **服务商**：阿里云 CosyVoice **v3-flash**（强制版本，不换 3.5）
- **支持方言音色**：普通话、四川话、东北话
- **健康助手**：回复生成时**预合成语音**（并行调用 LLM + TTS），用户点播放秒开
- **陪伴助手**：**按需生成**（用户点播放才调用 TTS），不预合成，节省成本
- **播放控制**：支持播放/暂停/继续，语音失败有降级提示

---

## 六、视频流与跌倒检测

### 6.1 架构总览

```
┌──────────────────────────────────────────────────────────┐
│                    USB 摄像头（老人端）                      │
│                         │                                  │
│                         ▼                                  │
│  ┌──────────────────────────────────────────────────┐    │
│  │  Fall-Down-Det/detector_server.py               │    │
│  │  （deeplearning 环境，独立进程）                  │    │
│  │                                                    │    │
│  │  1. OpenCV 抓帧（USB 摄像头，CAM_INDEX=1）       │    │
│  │  2. YOLOv8 推理（best.pt，CONF=0.4）             │    │
│  │  3. 连续帧确认（5帧窗口，4帧检出才告警）          │    │
│  │  4. 画框 + JPEG 编码（质量62，宽640）            │    │
│  │  5. WebSocket /ws/stream 推流（目标15fps）       │    │
│  │  6. 跌倒确认 → 截图 + POST 告警到业务后端          │    │
│  └──────────────┬───────────────────────┬───────────┘    │
│                 │                       │                │
│    WebSocket    │                       │ HTTP POST      │
│    (JPEG帧)     │                       │ (type=fall)    │
│                 ▼                       ▼                │
│  ┌─────────────────────────┐  ┌───────────────────────┐ │
│  │  前端监控页              │  │  FastAPI 业务后端      │ │
│  │  pages/monitor/monitor  │  │  /api/alerts          │ │
│  │  （仅展示画面，不跑模型）│  │  创建跌倒告警         │ │
│  └─────────────────────────┘  └──────────┬────────────┘ │
│                                            │              │
│                              ┌─────────────┴──────────┐   │
│                              ▼                        ▼   │
│                    子女端消息中心              社区端工单列表 │
│                    （弹窗警示+截图）          （紧急工单）   │
│                    跌倒记录列表（截图+时间）                  │
└──────────────────────────────────────────────────────────┘
```

### 6.2 跌倒检测服务（detector_server.py）

#### 6.2.1 技术栈

- **模型**：YOLOv8n（自定义训练，`runs/train/yolo26n_fall/weights/best.pt`）
- **推理框架**：Ultralytics
- **视频采集**：OpenCV（USB 摄像头）
- **推流协议**：WebSocket（FastAPI）
- **运行环境**：deeplearning conda 环境

#### 6.2.2 核心参数

| 参数 | 值 | 说明 |
|---|---|---|
| `CONF` | 0.4 | 检测置信度阈值 |
| `CONFIRM_WIN` | 5 | 确认窗口帧数 |
| `CONFIRM_NEED` | 4 | 窗口内至少4帧检出才告警 |
| `CLEAR_NEED` | 5 | 告警中连续5帧无检出则解除 |
| `TARGET_FPS` | 15 | 推流目标帧率 |
| `JPEG_QUALITY` | 62 | JPEG 质量（局域网流畅优先） |
| `FRAME_W` | 640 | 帧宽度 |
| `CAM_INDEX` | 1 | USB 摄像头索引（环境变量可改） |

#### 6.2.3 防误触机制

- **连续帧确认**：不是单帧检出就告警，而是 5 帧窗口内至少 4 帧检出跌倒才确认告警
- **告警冷却**：确认告警后，一定时间内不重复截图和告警（防止误触导致弹消息太多）
- **自动解除**：告警中连续 5 帧无检出则自动解除告警状态

#### 6.2.4 跌倒告警流程

1. 检测到跌倒（连续帧确认）
2. **截图**：保存跌倒帧到 `alert_shots/` 目录，文件名带时间戳
3. **POST 告警**：调用业务后端 `/api/alerts`，type=fall，附带截图路径和时间
4. **三端联动**：
   - 子女端：消息中心弹窗警示（红色），跌倒记录列表新增一条（截图+时间+日期）
   - 社区端：工单列表出现紧急工单（置顶标红）
   - 老人端：隐藏跌倒记录（老人不需要看）

### 6.3 前端监控页（pages/monitor/monitor）

- **老人端**：仅展示监控画面，不显示跌倒记录
- **子女端**：功能最全，监控画面 + 跌倒记录列表（截图+时间+日期），点击记录查看详情
- **社区端**：不需要监控画面，只需要跌倒提醒+截图

**前端只负责展示画面，所有模型推理在后端完成**，保证前端流畅。

### 6.4 Flask 视频后端（smart_elderly_care_backend/app.py）

- 提供监控页面 HTML（供浏览器查看）
- API 转发（跌倒告警、截图访问）
- 端口 5001
- 运行环境：deeplearning

---

## 七、三端联动机制

### 7.1 角色切换

- 启动页选择身份 → 写入全局状态（app.js globalData）+ 本地 storage
- 我的页面提供切换身份入口（方便演示反复切换）
- 切换身份后首页立即刷新，底部 tabBar 按角色显示不同 tab
- 重置模拟数据：演示前恢复初始 mock 数据

### 7.2 消息通道（三端隔离）

| 通道 | 发送方 | 接收方 | 说明 |
|---|---|---|---|
| `family` | 老人 ↔ 子女 | 老人端/子女端 | 家人对话，独立于其他通道 |
| `worker` | 老人 ↔ 社区 | 老人端/社区端 | 老人与社区工作人员对话，不与家人混淆 |
| `notice` | 社区/系统 → 老人 | 老人端 | 社区通知、服务完成通知、公告 |

**关键设计**：老人端有来自子女的消息和来自社区的消息，未读计数分开统计，点击未读消息时如果两类都有，让用户选择查看哪一类，不会直接跳错。

### 7.3 告警联动

| 告警类型 | 触发方 | 老人端 | 子女端 | 社区端 |
|---|---|---|---|---|
| SOS 求助 | 老人长按 SOS | 显示求助进度 | 红色告警弹窗 | 紧急工单置顶 |
| 跌倒检测 | 后端 YOLO 检测 | 隐藏 | 弹窗+截图+跌倒记录 | 紧急工单 |
| 漏服提醒 | 到点未确认吃药 | 语音提醒 | 漏服告警 | - |
| 血压异常 | 健康记录异常值 | 红色标记 | 告警 | - |
| 服务完成 | 社区标记完成 | 确认提醒 | 服务完成通知 | - |

### 7.4 订单联动

1. 老人端/子女端下单服务预约
2. 社区端工单列表出现新工单（待接单）
3. 社区人员接单 → 出发 → 完成服务 → 上传照片 → 填写记录
4. 老人端收到确认提醒，子女端看到服务完成
5. 老人确认完成并评价

---

## 八、环境依赖与启动方式

### 8.1 环境总览

项目使用**两个独立的 conda 环境**，都在 D 盘，不污染 C 盘：

| 环境 | 路径 | 用途 | 运行服务 |
|---|---|---|---|
| **base** | `D:\79458\Documents\anaconda3\python.exe` | 业务后端（FastAPI） | 智能体/数据库/API/ASR/TTS/定时任务 |
| **deeplearning** | `D:\79458\Documents\anaconda3\envs\deeplearning\python.exe` | 视频/AI 模型 | 跌倒检测/YOLOv8/OpenCV/视频推流 |

### 8.2 base 环境依赖（requirements.txt）

```
fastapi              # Web 框架
uvicorn              # ASGI 服务器
sqlalchemy           # ORM
pydantic             # 数据验证
pyttsx3              # 本地 TTS（备用）
apscheduler          # 定时任务
requests             # HTTP 请求
dashscope            # 阿里云大模型 API（Qwen-Flash + ASR + TTS）
python-multipart     # 文件上传
numpy                # 数值计算
imageio-ffmpeg       # 视频处理
```

### 8.3 deeplearning 环境依赖

```
ultralytics          # YOLOv8
opencv-python        # OpenCV
fastapi              # WebSocket 推流
uvicorn              # ASGI
flask                # 视频后端
numpy                # 数值计算
requests             # HTTP 请求
librosa              # 音频处理（ogg转mp3用）
soundfile            # 音频读写
```

### 8.4 一键启动

在 `D:\桌面\houduan` 目录下，右键"使用 PowerShell 运行"或执行：

```powershell
.\start_all.ps1
```

会自动弹出两个窗口：

| 窗口 | 服务 | 端口 | 环境 | 访问地址 |
|---|---|---|---|---|
| 窗口1 | FastAPI 业务后端 | 8000 | base | http://127.0.0.1:8000/docs |
| 窗口2 | Flask 视频后端 | 5001 | deeplearning | http://127.0.0.1:5001/ |

**跌倒检测服务**需要单独启动（如果需要视频监控和跌倒检测）：

```powershell
cd D:\桌面\houduan\Fall-Down-Det
D:\79458\Documents\anaconda3\envs\deeplearning\python.exe detector_server.py
```

停止服务：直接关闭弹出的窗口即可，或执行 `.\stop_all.ps1`。

### 8.5 前端启动

用微信开发者工具打开 `D:\桌面\WeChat1\smart_elderly_care_miniprogram`，编译运行。

**注意**：小程序需要在微信公众平台备案，开发阶段可在开发者工具中关闭域名校验（详情→本地设置→不校验合法域名）。

---

## 九、API 接口概览

### 9.1 业务后端（端口 8000）

| 模块 | 接口 | 方法 | 说明 |
|---|---|---|---|
| 用户 | `/api/users/register` | POST | 注册 |
| 用户 | `/api/users/login` | POST | 登录 |
| 用户 | `/api/users/profile` | GET/PUT | 获取/编辑资料 |
| 用户 | `/api/users/switch-role` | POST | 切换角色 |
| 健康 | `/api/health/records` | GET/POST | 健康记录列表/新增 |
| 健康助手 | `/api/health-agent/chat` | POST | 健康助手对话（SSE 流式） |
| 健康助手 | `/api/health-agent/explain` | POST | 术语解释（独立分支） |
| 陪伴助手 | `/api/companion/chat` | POST | 陪伴助手对话 |
| 用药 | `/api/medicines` | GET/POST | 用药计划 |
| 用药 | `/api/medicine-logs` | GET/POST | 用药确认记录 |
| 告警 | `/api/alerts` | GET/POST | 告警列表/创建 |
| 告警 | `/api/alerts/{id}/handle` | POST | 标记处理 |
| 消息 | `/api/messages` | GET/POST | 消息列表/发送 |
| 订单 | `/api/orders` | GET/POST | 订单列表/创建 |
| 订单 | `/api/orders/{id}/status` | PUT | 更新订单状态 |
| ASR | `/api/asr` | POST | 语音识别（上传音频文件） |
| TTS | `/api/tts` | POST | 语音合成（返回音频URL） |

### 9.2 跌倒检测服务

| 接口 | 方法 | 说明 |
|---|---|---|
| `/ws/stream` | WebSocket | 视频流推送（JPEG 帧 + 状态 JSON） |
| `/api/alerts` (转发) | POST | 跌倒告警上报到业务后端 |
| `/static/alert_shots/{filename}` | GET | 跌倒截图访问 |

---

## 十、适老化设计规范

### 10.1 全局规范

| 项目 | 标准 |
|---|---|
| 正文字号 | ≥32rpx |
| 标题字号 | ≥44rpx |
| 按钮高度 | ≥120rpx |
| 点击区域 | ≥88rpx |
| 主色 | 健康绿（#16a34a） |
| SOS/告警 | 红色（#dc2626） |
| 背景 | 浅色为主 |
| 文字 | 深色高对比 |
| 操作步骤 | ≤3步 |
| 重要操作 | 二次确认 |
| 一屏任务 | 尽量只放一个主要任务 |

### 10.2 三端差异化

| 端 | 设计特点 |
|---|---|
| **老人端** | 最大字号、最大按钮、最大间距，操作最简单，强调语音入口和悬浮助手，不放复杂信息 |
| **子女端** | 信息密度可稍高，强调信息摘要和快捷操作，专业术语标注和树形解释 |
| **社区端** | 工单流转和状态处理为主，表格/列表清晰，紧急工单置顶标红 |

---

## 十一、演示流程

### 11.1 完整演示链路

1. **启动**：打开小程序 → 选择身份（我是老人）
2. **老人首页**：查看天气/吃药进度/常用服务，点击 AI 悬浮球
3. **健康助手**：老人语音问"我今天血压怎么样"→ AI 调用工具查真实数据 → 口语化回复 + 语音播报
4. **SOS 求助**：长按 SOS 3秒 → 老人端显示求助进度 → 切换子女端 → 红色告警弹窗
5. **吃药提醒**：到点语音提醒 → 老人点"已吃药"→ 状态变更 + 语音"已记录，真棒"→ 子女端看到记录
6. **漏服告警**：老人不点 → 十分钟后子女端收到漏服告警
7. **陪伴助手**：老人说"我有点闷"→ 陪伴助手真实回复并陪聊 + 情感安慰
8. **服务预约**：老人/子女下单陪诊 → 社区端工单列表出现 → 社区接单 → 完成服务 → 老人端确认
9. **视频监控**：启动跌倒检测服务 → 子女端查看监控画面 → 模拟跌倒 → 后端 YOLO 检测 → 截图 + 告警 → 子女端弹窗 + 跌倒记录
10. **术语解释**：子女端问健康问题 → AI 回答中标注{{收缩压}}→ 点击展开独立分支解释 → 再点击{{靶器官}}→ 第二层解释 → 返回主线记忆完整
11. **切换身份**：我的页面 → 切换身份 → 选择子女/社区 → 首页立即刷新

---

## 十二、已知限制与后续规划

### 12.1 当前限制

| 限制 | 说明 |
|---|---|
| 小程序备案 | 生产环境需要在微信公众平台备案，开发阶段可关闭域名校验 |
| 音频格式 | 听书戏曲原 ogg 已转 mp3，后续新增音频建议直接用 mp3/m4a |
| 视频监控 | 目前用 USB 摄像头，生产环境可接网络摄像头/RTSP流 |
| 跌倒模型 | 自定义训练的 YOLOv8n，准确率取决于训练数据量，生产环境可优化模型 |
| AI 成本 | Qwen-Flash + TTS/ASR 调用有 API 成本，陪伴助手按需生成语音以节省成本 |
| 数据库 | 目前用 SQLite，生产环境可迁移 MySQL/PostgreSQL |

### 12.2 后续规划

- [ ] 接入真实 120/SOS 救援（目前只做流程演示）
- [ ] 接入真实地图定位（附近便民目前用模拟数据）
- [ ] 跌倒模型优化（更多训练数据，提升准确率）
- [ ] 视频监控支持多路摄像头
- [ ] 健康知识库扩充（更多 txt 文档）
- [ ] 陪伴助手增加更多娱乐功能（小游戏跳转、音乐推荐）
- [ ] 数据库迁移到 MySQL
- [ ] 部署到云服务器（目前本地运行）
- [ ] 小程序正式发布上线

---

## 十三、文件数量统计

| 类别 | 数量 |
|---|---|
| 前端页面模块 | 15 个 |
| 前端子页面 | 50+ 个 |
| 前端 JS 文件 | 50+ 个 |
| 真实图片 | 57 张 |
| 音乐文件 | 6 首 |
| 听书戏曲文件 | 2 首（可扩展至12首） |
| 后端 API 路由 | 14 个 |
| 后端服务模块 | 20+ 个 |
| 数据库模型 | 12 个 |
| 知识库文档 | 21 篇（41片段） |
| AI 助手 | 2 个（健康助手+陪伴助手） |
| Function Calling 工具 | 8 个 |

---

> **文档版本**：v1.0  
> **生成时间**：2026-09-16  
> **项目状态**：1.0 版本全量开发完成

---

## 十四、开发过程中遇到的问题与解决方案（踩坑记录）

> 本章记录整个项目开发过程中遇到的所有技术问题、尝试思路和最终解决方案。智能体相关问题最为复杂，记录最详细；其他模块按类别整理。每个问题包含：**问题描述 → 尝试思路 → 最终方案**。

---

### 14.1 智能体与 AI 对话（最重要，最费时间）

#### 问题 1：术语识别准确率——单纯正则很蠢，每次 LLM 抽取又费 token

**问题描述**：子女端健康助手回答中需要标注专业术语（如{{收缩压}}），让用户点击查看解释。但怎么判定哪些词值得标注？

**尝试思路**：
- 方案 A：用正则匹配医学词典——蠢，词典覆盖不全，普通词汇也会误标
- 方案 B：每次让 LLM 回答后再单独调用一次抽取术语——多消耗一轮 token，延迟增加
- 方案 C：在 System Prompt 里直接要求 LLM 回答时自行标注——零额外成本，但准确率依赖模型遵循指令的能力

**最终方案**：方案 C。在 System Prompt 里明确写规则："如果出现超出日常理解的专业术语，用 {{术语}} 标出来，可标 1~2 个最需要解释的，最多 2 个，普通词汇、动词、量词一律不要标，没有合适的术语就不要标。" Qwen-Flash 遵循指令能力足够，准确率可接受，零额外 token 成本。

---

#### 问题 2：术语树形解释的上下文隔离——分支对话不能污染主线

**问题描述**：点击术语展开独立分支解释，分支 B 不能继承主线 A 的全部上下文，主线 A 也不能读取分支 B 的内容。隔离逻辑一旦写错，主线记忆就被污染，变回普通对话。

**尝试思路**：
- 方案 A：前端维护树形结构，每个分支独立 conversation_id——复杂，前端状态管理难度高
- 方案 B：后端每个分支对话独立存储，用 parent_id 关联父子关系——数据库多一个字段，查询时按 conversation_id 隔离
- 方案 C：术语解释页调用独立 API，不传主线历史，只传术语本身和固定 System Prompt——最简单，彻底隔离

**最终方案**：方案 C。术语解释页 `/api/health-agent/explain` 是独立接口，只接收术语词，用固定的 `EXPLAIN_RULE` System Prompt（"当前是一个术语解释子对话…不点名具体某位老人、不调用数据工具"），完全不传入主线对话历史。主线对话的历史只在 `/api/health-agent/chat` 接口里使用。两层嵌套时，第二层解释同样调用 explain 接口，传入嵌套术语，不传第一层内容。彻底物理隔离，不可能污染。

---

#### 问题 3：术语解释延迟高达 5 秒以上——比主线慢很多

**问题描述**：主线回复很快（1~2秒），但点击术语解释要等 5 秒以上，用户体验差。

**尝试思路**：
- 方案 A：预加载——主线回答生成时，并行调用解释接口预生成术语解释，用户点进去直接看——但主线里可能标 1~2 个术语，每个都要预生成，token 消耗翻倍
- 方案 B：解释术语不开思考模式、不调工具——应该快，但实际还是慢
- 方案 C：排查链路——发现解释接口走的是和主线同一个 LLM 调用，但 System Prompt 更长，而且前端点击时才发起请求，没有预加载

**最终方案**：方案 B + C 结合。解释接口明确不开启 thinking 模式（`enable_thinking=False`），不调用任何工具，System Prompt 精简。同时接受延迟——因为术语解释是用户主动点击触发的，不是实时对话，3~5 秒延迟可接受。预加载方案因为 token 成本太高放弃（用户可能不点，预生成就浪费了）。

---

#### 问题 4：术语解释里又出现同一个术语——收缩压点进去又看见收缩压

**问题描述**：解释"收缩压"时，回答里又出现了 {{收缩压}}，用户点进去还是解释收缩压，虽然回答内容不一样但体验很奇怪。

**尝试思路**：
- 方案 A：System Prompt 里加"你正在解释的这个词本身不要标"——简单有效
- 方案 B：后处理过滤——把解释结果里的当前术语替换掉——复杂，可能误伤

**最终方案**：方案 A。在 `EXPLAIN_MARK_RULE` 里明确写："注意：你正在解释的这个词本身不要标。一定要输出一个 {{名词}}，不要省略。" 同时要求必须标一个**新的**嵌套术语（如解释收缩压时标{{靶器官}}），保证两层嵌套结构成立。

---

#### 问题 5：子女端回答首要人称错误——AI 把子女本人当成咨询对象

**问题描述**：子女端问"我妈今天怎么样"，AI 回答成"您的血压是…"，把李女士（子女）当成了被咨询身体状况的人，而不是向家属汇报老人（张奶奶）的情况。

**尝试思路**：
- 方案 A：System Prompt 里强调"所有健康状况的主体都是老人本人"——需要写得非常明确
- 方案 B：每次请求时把老人姓名作为参数传入，System Prompt 里引用——增加接口复杂度

**最终方案**：方案 A。在 `DAUGHTER_MARK_RULE` 里用大段文字强调："【视角】你在和子女（家属）对话，称呼对方用'您'。但所有健康状况、数据、判断的主体都是老人本人，始终围绕老人展开，例如'张奶奶的血压是…''张奶奶今天的服药进度…'；你是在向家属汇报老人的情况，不要把子女本人当成被咨询身体状况的人。" 修正后回答正确。

---

#### 问题 6：语音播报频繁 502/timeout——阿里服务不稳定

**问题描述**：健康助手语音播报经常失败，报错 `POST http://localhost:8000/api/tts 502 (Bad Gateway)` 或 `request:fail timeout`。之前好好的，后来偶尔能用，现在用不了。

**尝试思路**：
- 方案 A：怀疑阿里临时 token 过期没有刷新机制——检查代码，发现 DashScope SDK 内部管理 token，不需要手动刷新
- 方案 B：怀疑是链路复杂导致失效——健康助手预合成语音是并行调用 LLM+TTS，可能并发导致限流
- 方案 C：怀疑是阿里服务端问题——502 是网关错误，通常是服务端问题
- 方案 D：本地部署 TTS 模型——成本高，放弃

**最终方案**：多重降级。①健康助手采用**预合成方案**：LLM 回复生成时并行调用 TTS，用户点播放秒开，减少等待；②预取失败不阻断使用（"语音预取失败（不影响使用）"）；③播放失败有降级提示；④陪伴助手采用**按需生成**（用户点播放才调 TTS），节省成本；⑤接受阿里服务偶尔不稳定，演示时挑网络好的时候。根本原因是阿里 CosyVoice 服务端偶发 502，代码层无法彻底解决。

---

#### 问题 7：健康助手悬浮球页面和健康页面对话页不一致

**问题描述**：悬浮球点进去的 AI 对话页面和健康页面点进去的健康小助手页面不一样，悬浮球那种是回答后直接播报，健康页面那种是文字+选择播报。按理这两个是同一个东西，悬浮球只是更显眼的入口。

**尝试思路**：
- 方案 A：统一成悬浮球那种——用户不喜欢，更喜欢健康页面那种
- 方案 B：统一成健康页面那种，悬浮球跳转同一个页面——正确，两个入口指向同一个页面

**最终方案**：方案 B。悬浮球点击后跳转到 `pages/health/assistant/assistant`（健康助手对话页），不再有独立的悬浮球对话页。在健康助手页面原有基础上加语音输入按钮（之前没有），保留文字/语音播报选择，不改成回答后直接播报。对话内容持久化，翻出去再进来不丢失。

---

#### 问题 8：对话头像位置不对——头像在气泡前面

**问题描述**：老人对话这边的头像位置不对，头像跑到对话气泡前面去了，应该是气泡在左/右，头像在气泡外侧。

**尝试思路**：
- 方案 A：调整 CSS flex 方向和 order——检查发现是用户消息和 AI 消息的布局结构不一致
- 方案 B：统一用参考健康助手页面的头像位置——直接抄正确的布局

**最终方案**：方案 B。统一对话气泡布局：AI 消息头像在左、气泡在右；用户消息头像在右、气泡在左。用 `flex-direction: row` + `justify-content: flex-start/flex-end` 控制，头像固定尺寸，气泡 `flex: 1` 自适应。健康助手和陪伴助手两个页面统一修复。

---

#### 问题 9：术语解释页头像和主线不一致

**问题描述**：术语解释页用的是默认绿色无人头像，应该用 robot-2 图标区分。

**最终方案**：术语解释页 `pages/health/explain/explain` 头像换成 `/assets/images/robot-2.png`，与主线健康助手的 robot.png 区分，视觉上明确这是不同的对话分支。

---

#### 问题 10：RAG 知识库用什么形式填充——txt 还是结构化数据

**问题描述**：子女端健康助手加 RAG 知识库，知识库以什么形式填充？

**尝试思路**：
- 方案 A：用向量数据库（Chroma/Milvus）+ embedding API——重，需要额外服务，演示阶段没必要
- 方案 B：用 txt 纯文本，启动时加载到内存，TF-IDF + 关键词匹配检索——轻量，零外部依赖，适合演示
- 方案 C：用 JSON 结构化数据——不如 txt 灵活

**最终方案**：方案 B。21 篇健康科普 txt 文档（高血压、糖尿病、失眠、饮食、用药安全等），按段落切片成 41 个片段，存在 `agent/knowledge/` 目录。`knowledge_service.py` 启动时加载全部片段到内存，用 TF-IDF + 关键词匹配检索（`vector_store.py` 本地实现，不调用外部 embedding API）。通过 `search_knowledge` 工具按需触发，不每次都检索。轻量可运行，流程完整（文档→切片→检索→注入上下文→LLM回答），适合简历展示"会搭建 RAG"。

---

#### 问题 11：健康助手和陪伴助手边界混淆——陪伴助手回答医学问题

**问题描述**：陪伴助手（孙子角色）有时候会回答医学健康问题，和健康助手混淆。两个助手应该有明确边界。

**尝试思路**：
- 方案 A：陪伴助手 System Prompt 里明确"不涉及医学健康知识"——需要写清楚边界和引导逻辑
- 方案 B：前端拦截——检测到健康关键词自动跳健康助手——复杂，容易误判

**最终方案**：方案 A。陪伴助手 System Prompt 明确："不涉及医学健康知识，老人问健康问题时引导'这个问题健康助手更专业哦，我帮您转过去'。" 日常饮食（煮菜/食谱）在白名单内可以聊，但涉及健康功效时引导至健康助手。娱乐内容（故事/笑话）可以编造，新闻/生活常识必须联网搜索权威信息，找不到不建议回复，防止误导老人。

---

#### 问题 12：健康助手预合成语音 vs 陪伴助手按需生成——成本和体验的权衡

**问题描述**：两个助手的语音播放方式应该不一样。健康助手回复短、频率高，需要秒播；陪伴助手回复长、频率低，可以等。

**尝试思路**：
- 方案 A：两个都预合成——陪伴助手回复长，预合成 TTS 成本高，而且用户可能不听
- 方案 B：两个都按需——健康助手点播放要等 2~3 秒，体验差
- 方案 C：健康助手预合成（LLM+TTS 并行调用，用户点播放秒开），陪伴助手按需（用户点播放才调 TTS）——成本和体验平衡

**最终方案**：方案 C。健康助手后端在 LLM 生成回复时**并行调用 TTS** 预生成语音，前端收到回复时语音已经生成好，用户点播放秒开。陪伴助手**不预合成**，用户点播放按钮才调用 TTS，节省 API 成本（陪伴助手回复长，预合成浪费）。两个助手 API 都用阿里云 DashScope，但调用策略不同。

---

#### 问题 13：思考模式开不开——老人端 vs 子女端

**问题描述**：Qwen-Flash 支持 thinking 模式（深度思考），但会增加延迟。哪些端开？

**尝试思路**：
- 方案 A：都开——老人端等待时间长，老人不耐烦
- 方案 B：都不开——子女端回答不够专业深入
- 方案 C：仅子女端主线开，老人端和术语解释不开——子女端能等，需要专业回答；老人端要快，口语化就行

**最终方案**：方案 C。子女端健康助手主线对话开启 thinking 模式（`enable_thinking=True`），回答更专业、更结构化。老人端不开，需要快速口语化回复。术语解释页不开（固定模式，不需要思考）。思考过程前端流式展示，自动展开，对话框跟随思考滚动。

---

#### 问题 14：模型选型——Qwen-Flash vs plus vs R1

**问题描述**：健康助手用哪个模型？plus 据说效果好但 10 月 10 号下架；R1 深度思考但代价大；Qwen-Flash 有 thinking 模式。

**尝试思路**：
- 方案 A：用 plus——下架了不能用
- 方案 B：用 R1——需要换 API 平台，成本高，延迟大
- 方案 C：用 Qwen-Flash + thinking 模式——阿里云原生，和 ASR/TTS 同平台，成本低，延迟可接受

**最终方案**：方案 C。健康助手和陪伴助手都用阿里云 Qwen-Flash，子女端开启 thinking 模式。和 ASR（Paraformer）、TTS（CosyVoice）同属阿里云 DashScope 平台，一个 API Key 全搞定，不用跨平台。

---

#### 问题 15：TTS 模型版本——v3-flash vs 3.5-flash

**问题描述**：语音合成为什么用 v3-flash 不用 3.5-flash？3.5 应该更便宜还是效果有区别？

**尝试思路**：
- 方案 A：用 3.5-flash——可能更便宜，但效果不确定
- 方案 B：用 v3-flash——已验证可用，效果稳定

**最终方案**：方案 B。强制使用 CosyVoice v3-flash，在代码里写死模型版本（`model='cosyvoice-v3-flash'`），不自动升级。3.5-flash 效果未经充分验证，且可能存在兼容性问题。v3-flash 已在项目中稳定运行，方言音色（四川话/东北话）支持良好。

---

#### 问题 16：多轮记忆——健康助手数据库持久化，陪伴助手本地存储

**问题描述**：两个助手翻出去再进来对话内容就没了，需要持久化。

**尝试思路**：
- 方案 A：两个都存数据库——陪伴助手对话量可能大，数据库压力
- 方案 B：两个都存本地 storage——换设备数据丢失
- 方案 C：健康助手存数据库（专业对话需要完整记录），陪伴助手存本地 storage（最多50条，情感对话不需要长期保存）——平衡

**最终方案**：方案 C。健康助手对话消息持久化到 SQLite（`Conversation` + `ChatMessage` 表），每次调用 LLM 带上最近 N 轮历史，换设备/刷新不丢。陪伴助手对话存本地 `wx.setStorageSync('companion_chat_history')`，最多 50 条，超出滚动淘汰，刷新不丢但换设备丢失（情感对话可接受）。两个助手都支持"清空对话/重新开始"按钮。

---

#### 问题 17：预设问题快捷入口——固定式 vs 流动滚动式

**问题描述**：健康助手页面顶部预设问法，原来是上下三条固定式，用户想改成上下两行流动滚动效果。

**尝试思路**：
- 方案 A：上下两行相同问题左右流动——用户说"不是两行都是一样的问题然后一左一右"
- 方案 B：上下两行不同问题，同一个方向缓慢流动——正确

**最终方案**：方案 B。预设问题分成两组，上下两行各显示一组，同一个方向（从右往左）缓慢滚动，两行问题内容不一样。保留点击功能，点击后快速发送并得到回复。只改 UI 显示效果，不改动对话逻辑。

---

### 14.2 音频播放（音乐/听书戏曲）

#### 问题 18：ogg 格式没声音——微信小程序 iOS 不兼容

**问题描述**：听书戏曲文件是 ogg 格式，放进去后播放没声音。

**尝试思路**：
- 方案 A：代码层适配 ogg——微信小程序 InnerAudioContext 理论支持 ogg，但实际 iOS 上 vorbis 编码不兼容
- 方案 B：本地部署 TTS——不相关
- 方案 C：转码成 mp3——全平台兼容

**最终方案**：方案 C。用 anaconda deeplearning 环境的 librosa + soundfile 把两个 ogg 文件转成 mp3（帝女花、阿东读历史）。转码后代码里 url 从 `.ogg` 改成 `.mp3`。后续新增音频建议直接用 mp3/m4a，不用 ogg。

---

#### 问题 19：切换曲目进度共用——点第二个续着第一个进度

**问题描述**：暂停帝女花，点阿东，播放的还是帝女花的内容，进度也接着帝女花。再点一次阿东才是阿东的内容。

**尝试思路**：
- 方案 A：切换曲目时先 `stop()` 再设 src——stop() 是异步的，可能 stop 没完成就设了新 src
- 方案 B：设置 src 后延迟 `setTimeout` 再 play——延迟不可控
- 方案 C：`setData` 异步导致 `_playCurrent` 读旧 index——playTrack 里 setData({currentIndex}) 后立即调 _playCurrent()，此时 this.data.currentIndex 还是旧值
- 方案 D：每次播放都销毁旧实例创建新的 InnerAudioContext——彻底杜绝旧状态残留

**最终方案**：方案 C + D 结合。①`_playCurrent(idx)` 接受 index 参数，playTrack/prevTrack/nextTrack 把算好的 index 直接传进去，不依赖异步的 `this.data.currentIndex`；②每次播放都调用 `_createAudio()` 销毁旧实例、创建全新的 InnerAudioContext，然后设 src 播放。旧实例的进度、内容、状态完全不会残留。switchCategory 时也调用 `_createAudio()` 并清空 src，防止切换分类后点播放按钮还播上一首。音乐播放器和听书戏曲播放器两个都改成这个方案。

---

#### 问题 20：进度条不能拖拽——只有 bindchange 没有 bindchanging

**问题描述**：音频播放器的进度条拖不动，或者拖动后不跳转。

**尝试思路**：
- 方案 A：只有 `bindchange`（拖动结束触发）——拖动过程中进度条不更新，用户以为拖不动
- 方案 B：加 `bindchanging`（拖动中实时触发）——实时更新进度条显示，松手后 bindchange 执行 seek

**最终方案**：方案 B。slider 同时绑定 `bindchanging`（实时更新 `progress` 数据，进度条跟随手指移动）和 `bindchange`（松手后执行 `audio.seek()` 跳转）。滑块 `block-size` 从 18 加大到 20，更容易拖动。seek 时如果 `duration` 为 0（音频还没加载完），提示"音频加载中，请稍候"。

---

#### 问题 21：音乐文件命名和预设不一样——适配实际文件名

**问题描述**：预设音乐列表是 1.mp3~6.mp3，用户实际放的是中文文件名（"一支榴莲 - 海底.mp3"等）。

**最终方案**：直接读取 `assets/music/` 目录里的实际文件名，更新 `utils/music-config.js` 的 MUSIC_LIST，标题和作者从文件名解析（"歌手 - 歌名.mp3"）。6 首全部适配：海底/夜萤火虫和你/PDD洪荒之力/樱花泪/画心/青衣。duration 留空，播放时从音频文件自动读取实际时长。

---

### 14.3 前端 UI 与编译

#### 问题 22：WXSS 编译错误 unexpected ﻿ at pos 1——BOM 头

**问题描述**：`./pages/index/index.wxss(1:1): unexpected ﻿ at pos 1`，文件第一行报错。

**原因**：文件保存时带了 UTF-8 BOM 头（﻿），微信小程序 WXSS 编译器不识别 BOM。

**最终方案**：用编辑器以"UTF-8 无 BOM"格式重新保存文件。后续写文件时注意编码。

---

#### 问题 23：JS 语法错误 Expected ',' got 'ident'——注释后少逗号

**问题描述**：`pages/index/index.js:13:1 Expected ',', got 'ident'`，services 数组报错。

**原因**：`news: NEWS.slice(0, 4)` 后面跟了注释 `// 常用服务`，但少了逗号，导致下一行 `services:` 被当成语法错误。

**最终方案**：在 `news: NEWS.slice(0, 4)` 后面加逗号。

---

#### 问题 24：app.json 未找到页面对应 wxml——页面注册了但文件没创建

**问题描述**：`app.json: 未找到 ["pages"][1] 对应的 pages/news/index/index.wxml 文件`。

**原因**：app.json 里注册了页面，但对应的 wxml 文件还没创建。

**最终方案**：创建缺失的四个文件（js/wxml/wxss/json），确保 app.json 注册的每个页面都有完整的四件套。

---

#### 问题 25：家庭相册 wx.previewImage 黑屏转圈圈——本地包内图片兼容性

**问题描述**：家庭相册点开放大，用 `wx.previewImage` 黑屏转圈圈，打不开。

**尝试思路**：
- 方案 A：用 `wx.previewImage`——对本地包内图片偶发黑屏，兼容性差
- 方案 B：自定义全屏预览——用 swiper + image，左右滑动，底部页码，右上角关闭

**最终方案**：方案 B。弃用 `wx.previewImage`，改用自定义全屏预览组件：`swiper` 组件横向滑动所有照片，`image` 组件 `mode="aspectFit"` 完整显示，底部显示当前页码（1/4），右上角关闭按钮。稳定可靠，不黑屏。

---

#### 问题 26：翻牌记忆 wxml 不支持数组方法 includes——小程序模板限制

**问题描述**：翻牌记忆游戏翻不开，wxml 里用了 `flipped.includes(index)`。

**原因**：微信小程序 WXML 模板不支持直接调用数组方法（includes/indexOf 等），只能在 data 里预处理布尔值。

**最终方案**：cards 数据里每个卡片加 `isFlipped` 和 `isMatched` 布尔值，翻转时更新对应卡片的布尔值，wxml 里用 `{{item.isFlipped}}` 判断。

---

#### 问题 27：翻牌记忆 aspect-ratio 兼容性差——CSS 属性不支持

**问题描述**：翻牌卡片用了 `aspect-ratio: 1` 保证正方形，但小程序里不生效，卡片变成 3x4 网格比例不对。

**最终方案**：改用 `padding-bottom: 100%` 技巧实现正方形。卡片容器 `position: relative`，内部 `padding-bottom: 100%` 撑起高度，内容 `position: absolute` 铺满。兼容性好。

---

#### 问题 28：翻牌记忆配对成功不消掉——应该淡出消除

**问题描述**：配对成功的卡牌保持翻开状态，应该消掉。

**最终方案**：配对成功后设置 `isMatched: true`，CSS 里 `.card--matched` 加 `opacity: 0` + `transform: scale(0.8)` + `transition` 过渡动画，实现淡出缩小消除效果。

---

#### 问题 29：陪伴页统计数字 0 张照片——照片数据来源不一致

**问题描述**：陪伴主页顶部"0 张照片"，但家庭相册里有 4 张照片。

**原因**：陪伴主页计数从 `wx.getStorageSync('companion_photos')` 读取，但家庭相册的照片是硬编码在 album.js 里的，没有写入 storage，两边没打通。

**最终方案**：把家庭相册照片列表抽到公共配置 `utils/album-config.js`（PHOTOS 数组，4 张家人照片），陪伴主页和相册页都引用这份配置。陪伴主页照片数量从 `PHOTOS.length` 读取，不再从 storage 读。日记数量仍从 storage 读（用户动态添加），音乐数量从音乐公共配置读。以后加照片只需要改配置文件，两边自动同步。

---

#### 问题 30：悬浮球无法拖动/不吸附边缘/跑到导航栏下面

**问题描述**：AI 悬浮球无法拖动，拖动后不吸附边缘，有时候跑到底部导航栏下面。

**尝试思路**：
- 方案 A：固定位置——用户要求能拖动
- 方案 B：touchstart/touchmove/touchend 事件处理拖动——需要计算坐标、边界限制、吸附逻辑

**最终方案**：完整实现拖动逻辑：
- `touchstart`：记录起始坐标，标记拖动开始
- `touchmove`：实时更新 ballX/ballY，限制上下边界（顶部不超过状态栏，底部不超过导航栏）
- `touchend`：判断松手位置，靠左吸附到左边（ballX=12），靠右吸附到右边（ballX=窗口宽度-球宽-12），中间位置也吸附（不允许停在中间）
- 初始位置设在屏幕右侧中间偏上（用户指定位置）
- 上下边界限制：ballY 最小 100（不超过顶部），最大 窗口高度-300（不超过底部导航栏）

---

#### 问题 31：每日暖心语录爱心 emoji 颜色不可控——换成符号

**问题描述**：每日暖心语录用 💙 emoji 作为图标，但 emoji 颜色是固定的，无法通过 CSS 调整深浅，用户觉得太深。

**最终方案**：换成 ♡（U+2661 空心心形）符号，通过 CSS `color` 属性完全控制颜色。当前设为浅蓝色（#93c5fd），可随时调整。emoji 颜色不可控，符号颜色可控。

---

### 14.4 语音 ASR/TTS

#### 问题 32：方言几乎不可用——天津话读出来是普通话

**问题描述**：ASR 语音识别选了天津话方言，但识别出来还是普通话，五条全是普通话没区别，音色都一样。

**尝试思路**：
- 方案 A：检查方言参数——发现天津话可能不在支持列表
- 方案 B：测试多种方言——四川话、东北话、粤语、天津话
- 方案 C：粤语混合普粤很奇怪——放弃粤语

**最终方案**：保留四川话和东北话两种方言（验证可用），放弃天津话和粤语（天津话不支持，粤语混合普粤效果差）。方言选择在 `pages/profile/voice/voice` 页面，默认普通话，可选四川话/东北话。ASR 和 TTS 都使用对应的方言参数。

---

#### 问题 33：ASR 识别结果为空文本——录音格式问题

**问题描述**：`ASR成功 dialect=sichuan 格式=mp3 文本=''`，识别成功但文本为空。

**原因**：微信小程序录音默认格式是 aac，代码里指定 mp3 格式但实际录制的可能不是标准 mp3，阿里云 ASR 无法解析。

**最终方案**：检查录音参数，确保 `wx.getRecorderManager` 的 `format` 设为 `mp3`，采样率设为 16000（ASR 推荐）。录音完成后上传到后端，后端调用阿里云 Paraformer 识别。如果仍为空，在后端日志里打印音频文件大小和格式，排查是否真的录到了声音。

---

#### 问题 34：语音播报只能等不能暂停——加播放控制

**问题描述**：语音播报开始后只能白白等着放完，不能暂停。

**最终方案**：加播放/暂停/继续切换逻辑。点击播放按钮调用 `audio.play()`，播放中再点调用 `audio.pause()`，暂停后再点调用 `audio.play()` 继续。按钮图标随状态切换（▶/⏸）。健康助手和陪伴助手的语音播放都支持。

---

#### 问题 35：语音预下载缓存——清空对话释放

**问题描述**：健康助手预合成语音，每次对话都生成新的语音文件，积累多了占用空间。

**最终方案**：语音预生成后临时缓存，用户点击播放时使用缓存。清空对话时同时清除语音缓存，释放空间。不长期存储语音文件，因为健康助手回复可以重新生成。

---

### 14.5 三端联动与消息

#### 问题 36：护工完成服务信息混到家人对话里——消息通道未隔离

**问题描述**：社区护工完成服务的通知，出现在老人和子女的家人对话里面，老人端看则是护工的完成信息也成了孩子发过来的，消息混乱。

**原因**：所有消息都存在同一个通道里，没有区分发送方类型。

**最终方案**：消息表加 `channel` 字段，分三个通道：
- `family`：老人 ↔ 子女（家人对话）
- `worker`：老人 ↔ 社区工作人员（独立对话）
- `notice`：社区/系统 → 老人（通知、服务完成、公告）

护工完成服务的通知走 `notice` 通道，不出现在家人对话里。老人端有"常联系的人"里分别有家人（儿子/女儿）和社区王师傅，点击进入不同的对话页面，消息通道隔离，不混淆。

---

#### 问题 37：未读消息计数 bug——看完子女的护工的未读就消失了

**问题描述**：老人有来自子女的未读和来自社区的未读，点子女的信息看完了，护工的没点，首页未读信息就不见了。

**原因**：未读计数只有一个总数，看完一类就把总数清零了，没有分开统计。

**最终方案**：未读计数分开统计 `unreadChild`（子女未读）和 `unreadCommunity`（社区未读），首页显示总数 `unread = unreadChild + unreadCommunity`。点击未读消息时：
- 只有一类未读 → 直接进入对应对话
- 两类都有 → 弹出 ActionSheet 让用户选择"来自子女的回复（N条）"还是"来自社区的通知（N条）"
- 看完一类后只清零对应类的未读，另一类保留，首页总数正确更新

---

#### 问题 38：社区端老人留言入口点不开/接受不到信息——复用子女端逻辑

**问题描述**：社区端老人留言入口点不开，也接受不到老人发的信息。

**原因**：社区端消息页面没有实现老人留言对话功能，入口没有绑定跳转。

**最终方案**：复用子女端对话页面的逻辑，社区端新增 `pages/community/visit/visit` 页面，展示老人发送的消息列表，社区工作人员可以回复老人。消息走 `worker` 通道，与家人对话隔离。入口绑定到社区端消息页面的"老人留言"。

---

#### 问题 39：子女端告警点击已处理不消失/直接消失没有已处理记录

**问题描述**：子女端首页点击告警"已处理"，弹出来"已经标记处理了"但告警没有消失；后来修复了又变成直接消失，没有已处理记录。

**尝试思路**：
- 方案 A：标记处理后从列表移除——用户不满意，应该保留已处理记录
- 方案 B：标记处理后状态变为"已处理"，保留在列表里，视觉上变灰——正确

**最终方案**：方案 B。告警点击"标记处理"后，状态从"未处理"变为"已处理"，保留在告警列表里，视觉上变灰（降低不透明度），显示"已处理"标签。不直接删除，方便后续查看处理记录。首页当前状态也相应更新（告警数减少）。

---

#### 问题 40：跌倒告警没带截图和时间/点不开

**问题描述**：跌倒检测触发后，子女端收到告警但没有截图和时间，也点不开。

**最终方案**：跌倒检测服务确认告警后：
1. 截图保存到 `alert_shots/` 目录，文件名带时间戳
2. POST 告警到业务后端 `/api/alerts`，type=fall，附带截图路径、时间、日期
3. 子女端告警列表显示跌倒告警，带截图缩略图和时间，点击进入跌倒记录详情页（大图+完整时间+日期）
4. 跌倒记录列表在子女端监控页面，按时间倒序，每条显示截图+时间+日期
5. 老人端隐藏跌倒记录，社区端只需要跌倒提醒+截图（不需要监控画面）

---

#### 问题 41：跌倒检测误触弹窗 18 次——加冷却防误触

**问题描述**：跌倒检测有时候误判，连续弹窗 18 次，截图太多。

**最终方案**：加冷却时间机制。确认一次跌倒告警后，一定时间内（如 60 秒）不重复截图和告警，防止误触导致弹消息太多。同时连续帧确认机制（5帧窗口至少4帧检出才告警）也减少了误判。告警中连续5帧无检出则自动解除告警状态。

---

### 14.6 视频流与跌倒检测

#### 问题 42：视频画面非常卡几乎不可用——前端跑模型还是后端跑模型

**问题描述**：之前视频监控画面非常卡，几乎不可用。

**尝试思路**：
- 方案 A：前端跑模型——小程序前端性能差，跑 YOLO 必然卡
- 方案 B：后端跑模型，前端只展示画面——正确，前端只负责接收 JPEG 帧显示

**最终方案**：方案 B。所有模型推理在后端完成（`detector_server.py` 用 YOLOv8 推理），前端只通过 WebSocket 接收 JPEG 帧并显示。优化推流参数：JPEG 质量 62（局域网流畅优先），帧宽度 640，目标帧率 15fps。后端抓帧→推理→画框→JPEG编码→WebSocket推送，全流程在后端，前端零计算负担。

---

#### 问题 43：电脑摄像头检索新设备——USB 摄像头索引

**问题描述**：一开始用电脑内置摄像头，后来插上 USB 摄像头，检测不到新设备。

**最终方案**：`detector_server.py` 里 `CAM_INDEX` 参数控制摄像头索引，默认 1（USB 摄像头）。可通过环境变量 `CAM_INDEX` 修改。插上 USB 摄像头后，设置 `CAM_INDEX=1` 重启服务即可。如果索引不对画面黑屏，尝试 0/1/2 不同索引。

---

#### 问题 44：帧率提升——设备硬件限制

**问题描述**：视频帧率能不能提升？

**最终方案**：帧率主要受设备硬件限制（CPU 性能、USB 摄像头带宽）。代码层面已优化：JPEG 质量 62、帧宽 640、目标 15fps。如果需要更高帧率，需要更强的 CPU 或支持硬件加速的推理（如 TensorRT）。当前 12~15fps 对于跌倒检测和监控展示已足够。

---

### 14.7 后端与环境

#### 问题 45：GitLens 找不到 Git——环境变量

**问题描述**：VSCode 报错 "GitLens was unable to find Git. Please make sure Git is installed."

**原因**：Git 没有安装，或者安装了但不在 PATH 环境变量里。

**最终方案**：安装 Git for Windows，安装时勾选"Add Git to PATH"。或者在 VSCode 设置里指定 `git.path` 指向 git.exe 路径。

---

#### 问题 46：多环境运行不同解释器——base vs deeplearning

**问题描述**：项目有语音环境、深度学习环境等，能不能多个窗口运行不同解释器联动？会不会所有后端程序只能一个环境运行？

**最终方案**：完全可以多环境运行。项目用两个独立 conda 环境：
- **base 环境**（`D:\79458\Documents\anaconda3\python.exe`）：运行业务后端（FastAPI），不需要深度学习库
- **deeplearning 环境**（`D:\79458\Documents\anaconda3\envs\deeplearning\python.exe`）：运行视频/跌倒检测（YOLOv8+OpenCV），需要深度学习库

两个环境都在 D 盘，不污染 C 盘。通过 `start_all.ps1` 一键启动两个窗口，分别用不同解释器运行不同服务，通过 HTTP/WebSocket 联动。不需要所有程序在一个环境运行。

---

#### 问题 47：社区工作人员端切换身份失效

**问题描述**：社区工作人员端老是失效，刚刚还可以切换身份，后来不行了。

**原因**：切换身份时角色状态写入全局状态和本地 storage，但社区端的角色判断逻辑有 bug，或者 storage  key 不一致。

**最终方案**：统一角色切换逻辑，三个端（老人/子女/社区）共用同一套切换代码，在 `app.js` globalData 和本地 storage 里统一存储 `role` 字段。切换身份后首页立即刷新，底部 tabBar 按角色显示。我的页面提供切换身份入口，方便演示反复切换。

---

### 14.8 其他

#### 问题 48：小程序一开始应该选择身份而不是在"我的"里切换

**问题描述**：小程序点进去要在"我的"里切换身份，不太对，一开始应该打开小程序就先选择身份。

**最终方案**：新增启动页 `pages/splash/splash`，打开小程序首先进入启动页，用户选择"我是老人/我是家属/我是社区服务人员"，选择后写入全局状态和本地 storage，进入对应角色的首页。"我的"页面仍保留切换身份入口（方便演示反复切换），但首次进入默认走启动页选择。

---

#### 问题 49：子女端底部导航太简洁挤在一个页面

**问题描述**：子女端底部导航只有几个，内容都挤在一个页面。

**最终方案**：子女端底部导航增加 tab，参考老人端那种多 tab 布局。子女端首页分模块展示：老人状态卡片、今日告警列表、快捷操作区、健康助手入口、养老机构服务入口。各模块独立，不挤在一起。

---

#### 问题 50：个人信息放出来不专业不安全——收起来

**问题描述**：我的页面直接展示老人的个人信息（血型/慢病/过敏史等），不专业也不安全。

**最终方案**：我的页面只显示头像、姓名、"点击查看个人资料"小字提示，不直接展示敏感信息。点击后进入二级页面 `pages/profile/detail/detail`，展示完整个人资料并可编辑。老人端、子女端、社区端三端的个人资料页都改成这个模式。

---

#### 问题 51：长辈用户编辑资料和黄色按键功能重复

**问题描述**：老人端"个人信息"和黄色的"编辑资料"按钮功能重复。

**最终方案**：区分功能。"个人信息"入口进入资料查看页（只读展示），"编辑资料"按钮进入资料编辑页（可修改）。或者合并为一个入口，进入资料页后右上角有"编辑"按钮。老人端采用合并方案：一个入口进入资料页，可查看可编辑。

---

#### 问题 52：健康知识卡片点进去直接跳 AI 聊天——应该是详细知识页

**问题描述**：首页健康知识卡片（高血压/失眠/饮食）点进去直接跳到健康 AI 聊天页面，而不是详细的知识内容。

**最终方案**：健康知识卡片点击进入 `pages/knowledge/detail/detail` 详细知识页，展示该主题的完整科普内容（大图半隐背景+滑动效果+详细文字）。详细知识页底部有"问健康助手"按钮，点击才跳转到 AI 聊天页面。"更多健康知识"按钮也跳转到详细知识页或知识列表，不直接跳 AI。

---

#### 问题 53：搜索功能是摆设——实现服务搜索和问医生

**问题描述**：首页搜索栏只是摆设，输入后不能搜索。

**最终方案**：实现搜索功能。搜索栏点击后展开下拉搜索浮层，输入关键词实时匹配建议列表（医院攻略/吃药提醒/社区活动/健康知识/在线问诊等），点击建议跳转到对应页面。搜索确认后如果匹配到服务直接跳转，匹配到健康知识关键词跳转到对应知识详情页，匹配到医生相关跳转到在线问诊页。不是摆设。

---

#### 问题 54：字体太 AI 感太圆润——换正式字体

**问题描述**：当前字体太圆润，有 AI 感，想要正式一些的字体。

**最终方案**：尝试引入更正式的字体（如思源黑体/苹方）。但微信小程序自定义字体需要通过 `wx.loadFontFace` 加载网络字体，可能存在加载失败和编译问题。最终使用系统默认字体（iOS 苹方/Android 思源黑体），通过调整 font-weight（400/500/600/700）和 letter-spacing 来营造正式感，不依赖外部字体文件，避免加载失败。

---

#### 问题 55：系统设置方块色调不统一/夜间模式只模块换不全页面

**问题描述**：系统设置几个方块颜色不统一；夜间模式切换后只有模块之间换了颜色，不是全部页面都变深色。

**最终方案**：系统设置方块统一色调（同一色系不同深浅）。夜间模式因为涉及全部页面的 CSS 变量替换，工作量大，用户决定先不搞夜间模式，保持浅色模式。后续如果需要，采用全局 CSS 变量（`--bg-color`/`--text-color` 等）+ app.wxss 统一管理，切换时改变量，所有页面统一变色。

---

#### 问题 56：公告和代办两条一起——改成滚动式

**问题描述**：首页公告和今日代办两条信息一起显示，不滚动。

**最终方案**：公告条改成垂直滚动轮播（`swiper` + `vertical`），多条公告自动上下滚动，小喇叭图标 + 公告文字。今日代办是独立的吃药进度条，和公告分开显示，不混在一起。

---

#### 问题 57：圆形图标改成矩形方块

**问题描述**：常用服务图标用的圆形，用户想要一块一块的矩形。

**最终方案**：常用服务图标从圆形改成圆角矩形（`border-radius: 16rpx`），图片用 `mode="aspectFill"` 填满矩形，更有商用感，不幼稚。

---

### 14.9 问题统计

| 类别 | 问题数量 | 最复杂问题 |
|---|---|---|
| 智能体与 AI 对话 | 17 | 术语树形解释上下文隔离、语音播报 502、RAG 轻量实现 |
| 音频播放 | 4 | 切换曲目进度共用（每次新建实例解决） |
| 前端 UI 与编译 | 10 | 悬浮球拖动+吸附+边界、翻牌记忆兼容性 |
| 语音 ASR/TTS | 4 | 方言适配、ASR 空文本 |
| 三端联动与消息 | 6 | 消息三通道隔离、未读计数分开 |
| 视频流与跌倒检测 | 3 | 后端跑模型前端只展示 |
| 后端与环境 | 3 | 多环境运行 |
| 其他 | 10 | 启动页身份选择、个人信息收起 |
| **合计** | **57** | |

> 以上 57 个问题是整个 1.0 版本开发过程中遇到的主要技术问题，每个都经历了"问题描述 → 尝试思路 → 最终方案"的完整过程。其中智能体相关问题最为复杂，耗时最长，也最有技术价值（术语树形解释、RAG、Function Calling、多人设、思考模式等）。

---

> **文档版本**：v1.1（新增第十四章踩坑记录）  
> **最后更新**：2026-09-16
